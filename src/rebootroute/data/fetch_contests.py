from __future__ import annotations

import pandas as pd


def fetch_contests() -> pd.DataFrame:
    """No contest source is exposed until a verifiable official feed is configured."""
    return pd.DataFrame()
